# Next の rewrite が JSON API を転送する先(server 側のみ参照)。既定は localhost の uvicorn。
API_BASE=http://127.0.0.1:8765

# SSE はブラウザ→FastAPI 直 + CORS の方針(技術決定)。EventSource の接続先 base。
# 同一オリジン rewrite を使う JSON とは別に、SSE はこの base を直叩きする。
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8765
